import { ReactElement, FC } from "react";
import { StyledSection } from "../themed/StyledSection";

export const TextImageSectio: FC = (): ReactElement => {
    return <StyledSection>
        <p>Prova</p>
    </StyledSection>
}